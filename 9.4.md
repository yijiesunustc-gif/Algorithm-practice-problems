### 题目二

- **编号 / 来源**：LeetCode 297. 二叉树的序列化与反序列化（Serialize and Deserialize Binary Tree）
- **考点类型**：树 / 深度优先搜索（DFS）/ 广度优先搜索（BFS）/ 设计
- **难度评级**：⭐⭐⭐⭐ 困难

**题目描述：**

序列化是将一个数据结构或者对象转换为连续的比特位的操作，进而可以将转换后的数据存储在一个文件或者内存中，同时也可以通过网络传输到另一个计算机环境，采取相反方式重构得到原数据。

请设计一个算法来实现二叉树的序列化与反序列化。这里不限定你的序列 / 反序列化算法执行逻辑，你只需要保证一个二叉树可以被序列化为一个字符串并且将这个字符串反序列化为原始的树结构。

**示例 1：**

```
输入：root = [1,2,3,null,null,4,5]
输出：[1,2,3,null,null,4,5]
```

**示例 2：**

```
输入：root = []
输出：[]
```

**提示：**

- 树中结点数在范围 `[0, 10⁴]` 内
- `-1000 <= Node.val <= 1000`
### 题目二详解：二叉树的序列化与反序列化

#### 解题思路

本题要求设计两个互逆的操作：

- **序列化（serialize）**：将二叉树转换为字符串
- **反序列化（deserialize）**：将字符串还原为二叉树

关键在于：序列化时必须记录空节点（null）的位置，否则反序列化时无法确定树的结构。

有两种主流解法：

**解法一：深度优先搜索（DFS，前序遍历）**

- **序列化**：前序遍历二叉树，遇到节点记录其值，遇到空节点记录特殊标记（如 `"#"`），各值之间用逗号分隔。
  - 例如示例 1 的树序列化为：`"1,2,#,#,3,4,#,#,5,#,#"`
- **反序列化**：将字符串按逗号分割为列表，用一个指针（或队列）按顺序消费。
  - 遇到 `"#"` 返回 `nullptr`
  - 遇到数值创建节点，然后递归构建左子树和右子树
  - 因为前序遍历的顺序是 "根→左→右"，反序列化时按同样顺序消费即可完美还原

**解法二：广度优先搜索（BFS，层序遍历）**

- **序列化**：层序遍历，用队列，空节点也记录为 `"#"`。这就是 LeetCode 官方使用的格式。
  - 例如示例 1 序列化为：`"1,2,3,#,#,4,5,#,#,#,#"`
- **反序列化**：同样用队列，按层序还原。每次从队列取出一个节点，然后依次消费两个值作为它的左右孩子。

> 
> DFS 代码更简洁，递归逻辑清晰，是面试首选；BFS 与 LeetCode 官方格式一致，适合需要兼容平台格式的场景。

#### C++ 完整代码

**解法一：DFS 前序遍历（推荐，最简洁）**

```cpp
#include <string>
#include <sstream>
using namespace std;

struct TreeNode {
    int val;
    TreeNode *left;
    TreeNode *right;
    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
};

class Codec {
public:
    // 序列化：前序遍历，空节点用 "#" 标记
    string serialize(TreeNode* root) {
        if (!root) return "#";
        // 根 → 左 → 右，用逗号分隔
        return to_string(root->val) + "," 
             + serialize(root->left) + "," 
             + serialize(root->right);
    }

    // 反序列化
    TreeNode* deserialize(string data) {
        // 将字符串转为输入流，方便按逗号读取
        istringstream iss(data);
        return dfs(iss);
    }

private:
    TreeNode* dfs(istringstream& iss) {
        string val;
        // 按逗号读取一个值
        if (!getline(iss, val, ',')) return nullptr;
        if (val == "#") return nullptr; // 空节点
        
        TreeNode* node = new TreeNode(stoi(val));
        node->left = dfs(iss);  // 递归构建左子树
        node->right = dfs(iss); // 递归构建右子树
        return node;
    }
};
```

**解法二：BFS 层序遍历**

```cpp
#include <string>
#include <sstream>
#include <queue>
using namespace std;

struct TreeNode {
    int val;
    TreeNode *left;
    TreeNode *right;
    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
};

class Codec {
public:
    // 序列化：层序遍历
    string serialize(TreeNode* root) {
        if (!root) return "#";
        queue<TreeNode*> q;
        q.push(root);
        string result;
        
        while (!q.empty()) {
            TreeNode* node = q.front();
            q.pop();
            if (node) {
                result += to_string(node->val) + ",";
                q.push(node->left);
                q.push(node->right);
            } else {
                result += "#,";
            }
        }
        // 去掉末尾多余的逗号
        if (!result.empty()) result.pop_back();
        return result;
    }

    // 反序列化：层序还原
    TreeNode* deserialize(string data) {
        istringstream iss(data);
        string val;
        if (!getline(iss, val, ',') || val == "#") return nullptr;
        
        TreeNode* root = new TreeNode(stoi(val));
        queue<TreeNode*> q;
        q.push(root);
        
        while (!q.empty()) {
            TreeNode* node = q.front();
            q.pop();
            
            // 左孩子
            if (getline(iss, val, ',')) {
                if (val != "#") {
                    node->left = new TreeNode(stoi(val));
                    q.push(node->left);
                }
            }
            // 右孩子
            if (getline(iss, val, ',')) {
                if (val != "#") {
                    node->right = new TreeNode(stoi(val));
                    q.push(node->right);
                }
            }
        }
        return root;
    }
};
```

#### 复杂度分析

表格

| 解法 | 时间复杂度 | 空间复杂度 |
| --- | --- | --- |
| DFS 前序 | O (n)，每个节点访问一次 | O (h)，递归栈深度为树高 h，最坏 O (n) |
| BFS 层序 | O(n) | O (n)，队列最大宽度，最坏 O (n)（完美二叉树最底层约 n/2） |

#### 优化方向与同类题拓展

- **序列化格式优化**：可以用更紧凑的编码方式（如二进制序列化）减少字符串长度，但题目不限制格式，字符串即可。
- **同类题**：
  - LeetCode 449. 序列化和反序列化二叉搜索树（利用 BST 特性，可用中序 + 前序还原，无需记录 null）
  - LeetCode 428. 序列化和反序列化 N 叉树
  - LeetCode 652. 寻找重复的子树（序列化子树用于哈希比较）
  - 剑指 Offer 37. 序列化二叉树（同题）
  - 剑指 Offer 54. 二叉搜索树的第 k 大节点
- **技巧总结**：
  - 序列化二叉树的核心是**记录空节点**，否则结构信息丢失。
  - DFS 前序序列化 + 递归反序列化是最优雅的组合，序列化和反序列化的遍历顺序必须一致。
  - 用 `istringstream` + `getline(..., ',')` 按分隔符读取是 C++ 中处理序列化字符串的标准技巧。